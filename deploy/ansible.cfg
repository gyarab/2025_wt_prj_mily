# Ansible config pro deploy/seed playbooky. Načítá se automaticky, když ansible
# běží z adresáře deploy/ (tj. z GitHub Actions i ručně).
[defaults]
inventory = inventory.ini
result_format = yaml
gathering = explicit
host_key_checking = False
deprecation_warnings = False
